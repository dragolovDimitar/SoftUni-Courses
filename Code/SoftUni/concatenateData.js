function concatenateData(input){
    let name = input[0];
    let familia = input[1];
    let age = input [2];
    let town = input [3];

    console.log(`You are ${name} ${familia}, a ${age}-years old person from ${town}.`);
}

concatenateData(["Mitko","Dragolov", 24, "Yambol",]);